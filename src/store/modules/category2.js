import { category,categoryList } from '../../api'

const state = {
    category: [],  //左侧导航栏数据
    categoryList: []  //右侧内容区域
}

const mutations = {
    
}

const actions = {

}

const getters = {

}

export default{
    state,
    mutations,
    actions,
    getters
}